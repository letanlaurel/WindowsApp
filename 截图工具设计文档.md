# 截图贴图工具 设计文档

> 版本：v1.0 草案
> 平台：Windows 11（兼容 Windows 10 1809+）
> 日期：2026-09-03

---

## 1. 产品概述

### 1.1 定位
一款轻量级 Windows 桌面截图工具，核心体验对标 Snipaste / QQ 截图：
- 一键截取屏幕区域
- 截图可"钉"在桌面成为**置顶悬浮窗**，随意拖动、缩放、旋转
- 截图可保存到本地文件（PNG / JPG / BMP / 剪贴板）
- 截图上可标注：文字、箭头、矩形、椭圆、画笔、马赛克、序号标记

### 1.2 目标用户
- 开发者、设计师、文档撰写者、客服、日常办公用户
- 需要"把一张图钉在屏幕上对照参考"的场景（抄代码、对设计稿、填表）

### 1.3 核心卖点
| 卖点 | 说明 |
|------|------|
| 贴图（Pin） | 截图以无边框置顶窗体悬浮，永不遮挡工作 |
| 标注 | 丰富的矢量标注，可二次编辑、撤销重做 |
| 轻量 | 常驻托盘，内存占用低，秒级启动 |
| 全局热键 | 任意界面一键唤起 |

---

## 2. 功能需求

### 2.1 功能模块总览

```
截图工具
├── 截图（Capture）
│   ├── 区域截图（拖拽框选 + 智能窗口识别）
│   ├── 全屏截图
│   ├── 窗口截图
│   └── 延时截图
├── 标注（Annotate）
│   ├── 文字
│   ├── 箭头
│   ├── 矩形 / 椭圆 / 直线
│   ├── 画笔（自由绘制）
│   ├── 马赛克 / 高亮
│   └── 序号标记（步骤编号）
├── 贴图（Pin）
│   ├── 钉住为置顶悬浮窗
│   ├── 拖动 / 缩放 / 旋转 / 半透明
│   └── 取消贴图、关闭
├── 输出（Export）
│   ├── 保存到文件（PNG/JPG/BMP）
│   ├── 复制到剪贴板
│   └── 另存 / 快速保存（自动命名）
└── 系统
    ├── 全局热键
    ├── 系统托盘
    └── 设置（开机自启、保存路径、快捷键自定义）
```

### 2.2 截图功能

#### 2.2.1 区域截图（核心）
1. 按下全局热键（默认 `F1`）或点击托盘菜单"截图"
2. 屏幕变暗显示半透明遮罩层（全屏无边框窗体）
3. 鼠标拖拽框选区域，实时显示选区尺寸（宽 × 高）
4. **智能窗口识别**：鼠标悬停自动吸附到窗口边界（基于 UI Automation / DWM 枚举窗口矩形）
5. 选区边缘支持 8 个控制点微调；方向键 1px 精调，`Shift+方向键` 10px
6. 框选完成后弹出**标注工具条**（位于选区下缘/右缘）
7. 用户可选择：✅完成 / 📌钉住 / 💾保存 / 📋复制 / ❌取消（`Esc`）

#### 2.2.2 全屏截图
- 热键 `F3` 直接截取当前主屏 / `Ctrl+F3` 所有屏幕
- 多显示器：逐屏合成一张大图

#### 2.2.3 窗口截图
- 热键触发后点击目标窗口，自动捕获该窗口客户区（含圆角透明背景）

#### 2.2.4 延时截图
- 设置 N 秒倒计时后自动触发（用于捕获菜单、悬浮提示等易消失元素）

### 2.3 标注功能

标注在截图确认前的编辑态进行，所有标注为**矢量对象**，可选中、移动、改色、删除，支持撤销/重做。

| 工具 | 快捷键 | 说明 |
|------|--------|------|
| 选择 | `V` | 选中并移动/缩放已有标注 |
| 文字 | `T` | 点击插入文本框，支持字体、字号、颜色、加粗、描边 |
| 箭头 | `A` | 拖拽绘制箭头，可调粗细、颜色、线型（实线/虚线） |
| 矩形 | `R` | 拖拽矩形框，描边/填充可选 |
| 椭圆 | `O` | 拖拽椭圆/正圆（按住 Shift） |
| 直线 | `L` | 直线，Shift 强制水平/垂直/45° |
| 画笔 | `P` | 自由手绘 |
| 马赛克 | `M` | 涂抹打码，可调强度/像素块大小 |
| 高亮 | `H` | 半透明荧光笔 |
| 序号 | `N` | 自动递增的步骤编号圆圈 |
| 撤销 | `Ctrl+Z` | |
| 重做 | `Ctrl+Y` | |

**通用属性**（工具条二级面板）：
- 颜色：预设 8 色 + 自定义取色
- 粗细：细 / 中 / 粗
- 阴影开关

### 2.4 贴图功能（Pin）

#### 2.4.1 行为
- 截图确认后点击"📌钉住"，截图生成一个**无边框、置顶、可拖动**的悬浮窗
- 支持同时钉多张，每张独立悬浮窗
- 悬浮窗初始大小 = 截图实际大小，位置 = 截图时所在屏幕位置（可配置）

#### 2.4.2 悬浮窗交互
| 操作 | 方式 |
|------|------|
| 拖动 | 鼠标按住图片任意处拖动 |
| 缩放 | `Ctrl + 滚轮`（10%~500%） |
| 旋转 | `Shift + 滚轮` 或菜单（90° 步进） |
| 透明度 | `Alt + 滚轮` 或右键菜单滑块 |
| 关闭 | 双击 / `Esc` / 右键菜单"关闭" |
| 取消置顶 | 右键菜单切换"始终置顶" |
| 另存 | 右键菜单"保存为图片" |
| 复制 | 右键菜单"复制到剪贴板" |
| 缩略 | 右键"收起为缩略图条"，点击还原 |

#### 2.4.3 贴图管理
- 托盘菜单"贴图列表"：展示所有钉住的图，可定位、关闭、全部清除
- 关闭程序时可选择保留贴图会话（下次启动恢复）

### 2.5 输出功能

#### 2.5.1 保存到本地
- 默认目录：`图片\截图\`（可在设置中修改）
- 自动命名规则：`截图_2026-09-03_15-30-25.png`（支持自定义模板：日期、时间、序号）
- 格式：PNG（默认无损）/ JPG（可调质量）/ BMP
- 快速保存热键：标注工具条一键保存，不打断流程

#### 2.5.2 剪贴板
- 截图/标注完成后自动复制到剪贴板（可在设置关闭）
- 支持复制为带透明通道的 PNG（DIB + PNG 双格式写入剪贴板）

### 2.6 系统功能

#### 2.6.1 全局热键（默认，均可自定义）
| 功能 | 默认键 |
|------|--------|
| 区域截图 | `F1` |
| 钉住剪贴板图片 | `F2` |
| 全屏截图 | `F3` |
| 显示/隐藏所有贴图 | `F4` |
| 打开设置 | `Ctrl+,` |

#### 2.6.2 托盘
- 常驻系统托盘，左键截图，右键菜单
- 菜单项：截图 / 贴图列表 / 打开保存目录 / 设置 / 开机自启 / 退出

#### 2.6.3 设置项
- 常规：开机自启、语言、主题（跟随系统/明/暗）
- 热键：自定义各快捷键
- 保存：默认目录、命名模板、默认格式、JPG 质量
- 截图：智能窗口识别开关、放大镜开关、遮罩透明度
- 贴图：默认置顶、默认透明度、关闭时是否保留会话
- 输出：截图后自动复制、保存后提示音

---

## 3. 技术方案

### 3.1 技术选型

| 项目 | 选型 | 理由 |
|------|------|------|
| 语言/框架 | **C# + WPF（.NET 8）** | 无边框窗体、Per-Monitor DPI、矢重绘标注（InkCanvas/Adorner）成熟 |
| 屏幕捕获 | `Graphics.CopyFromScreen` / **Graphics Capture API** | 后者支持单窗口捕获、不闪烁（Win10 1809+） |
| 全局热键 | Win32 `RegisterHotKey` (P/Invoke) | 系统级，稳定 |
| 置顶窗体 | WPF `Topmost` + `WindowStyle=None` | 天然支持贴图 |
| 窗口识别 | UI Automation + DWM API | 智能吸附窗口边界 |
| 图像处理 | `System.Drawing.Common` / `WriteableBitmap` | 高性能像素操作（马赛克） |
| 配置存储 | JSON 文件（`%AppData%`） | 免安装、易迁移 |
| 单实例/托盘 | `Mutex` + `Hardcodet.NotifyIcon.Wpf` | 成熟开源托盘组件 |
| 日志 | Serilog（滚动文件） | 便于排障 |

> 备选：WinUI 3（更现代 UI 但贴图窗体/热键支持稍繁琐）；Flutter/Qt（跨平台但 Windows 集成度低）。**推荐 WPF**。

### 3.2 关键架构（分层）

```
┌─────────────────────────────────────────┐
│  UI 层 (Views)                          │
│  截图遮罩窗 / 标注工具条 / 贴图窗 / 设置窗 │
├─────────────────────────────────────────┤
│  应用服务层 (Services)                  │
│  CaptureService / PinService /          │
│  AnnotationService / HotkeyService /    │
│  StorageService / TrayService           │
├─────────────────────────────────────────┤
│  领域层 (Domain)                        │
│  Annotation 对象模型 / 命令(撤销重做)    │
├─────────────────────────────────────────┤
│  基础设施层 (Infrastructure)            │
│  Win32 互操作 / Graphics Capture /      │
│  配置文件 / 日志                        │
└─────────────────────────────────────────┘
```

设计原则：
- **MVVM** 模式，View 与逻辑解耦
- 依赖注入（`Microsoft.Extensions.DependencyInjection`），禁止内部 `new()` 下层依赖
- 标注采用**命令模式**实现撤销/重做栈

### 3.3 核心模块设计

#### 3.3.1 截图流程（CaptureService）
```
Hotkey(F1)
   │
   ▼
抓取全屏位图（多屏合成） ──► 显示遮罩窗体(全屏/无边框/置顶)
   │
   ▼
用户框选/窗口识别 ──► 裁剪选区位图
   │
   ▼
进入标注编辑态（AnnotationService 接管）
   │
   ├── 钉住 ──► PinService 创建贴图窗
   ├── 保存 ──► StorageService 写文件
   └── 复制 ──► 写剪贴板
```

#### 3.3.2 标注对象模型
```csharp
// 标注基类（矢量、可序列化）
abstract class Annotation {
    Guid Id;
    Color StrokeColor;
    double Thickness;
    bool HasShadow;
    abstract void Render(DrawingContext dc, double scale);
    abstract Rect GetBounds();
}

class TextAnnotation    : Annotation { string Text; Point Position; FontInfo Font; }
class ArrowAnnotation   : Annotation { Point Start, End; ArrowHeadStyle Head; }
class RectAnnotation    : Annotation { Rect Bounds; Color? Fill; }
class EllipseAnnotation : Annotation { Rect Bounds; Color? Fill; }
class LineAnnotation    : Annotation { Point Start, End; }
class PenAnnotation     : Annotation { List<Point> Points; }
class MosaicAnnotation  : Annotation { List<Rect> Regions; int BlockSize; } // 直接改像素
class HighlightAnnotation: Annotation { List<Point> Points; double Opacity; }
class NumberAnnotation  : Annotation { int Number; Point Center; }
```

撤销/重做：
```csharp
interface ICommand { void Execute(); void Undo(); }
class AddAnnotationCommand    : ICommand { ... }
class MoveAnnotationCommand   : ICommand { ... }
class ModifyPropertyCommand   : ICommand { ... }
class DeleteAnnotationCommand : ICommand { ... }
// UndoRedoStack 维护两个栈
```

#### 3.3.3 贴图窗（PinService）
```csharp
class PinWindow : Window {
    // WindowStyle=None, AllowsTransparency=true, Topmost=true
    BitmapSource Image;
    double Scale = 1.0;      // Ctrl+滚轮
    double Rotation = 0;     // Shift+滚轮
    double Opacity = 1.0;    // Alt+滚轮
    bool AlwaysOnTop = true;
}
// PinService 维护 List<PinWindow>，支持序列化会话
```

#### 3.3.4 全局热键（HotkeyService）
```csharp
// Win32 RegisterHotKey 封装，支持注销/重注册（设置修改热键时）
[DllImport("user32.dll")]
static extern bool RegisterHotKey(IntPtr hWnd, int id, uint fsModifiers, uint vk);
// 消息循环 WndProc 监听 WM_HOTKEY
```

### 3.4 数据与配置

配置文件 `%AppData%\SnipPin\config.json`：
```json
{
  "general": { "autoStart": true, "language": "zh-CN", "theme": "system" },
  "hotkeys": {
    "capture": "F1", "pinClipboard": "F2",
    "fullScreen": "F3", "togglePins": "F4"
  },
  "save": {
    "directory": "%Pictures%\\SnipPin",
    "nameTemplate": "截图_{yyyy-MM-dd}_{HH-mm-ss}",
    "format": "png", "jpgQuality": 90
  },
  "capture": { "windowSnap": true, "magnifier": true, "maskOpacity": 0.4 },
  "pin": { "alwaysOnTop": true, "defaultOpacity": 1.0, "restoreSession": true }
}
```

### 3.5 非功能性需求

| 项 | 指标 |
|----|------|
| 启动时间 | 冷启动 < 1s，截图唤起 < 300ms |
| 内存 | 常驻 < 80MB（不含贴图） |
| DPI | Per-Monitor V2 感知，多屏混合 DPI 正确 |
| 兼容 | Windows 10 1809+ / Windows 11 |
| 打包 | MSIX / 绿色单文件（自包含发布） |

---

## 4. UI / UX 设计

### 4.1 截图遮罩界面
```
┌────────────────────────────────────────┐
│ ░░░░░░░░░░░ 半透明遮罩 ░░░░░░░░░░░░░░ │
│ ░░░┌──────────────┐░░░░░░░░░░░░░░░░░░ │
│ ░░░│   选区(清晰)   │░░ 1920×1080 ░░░ │  ← 尺寸提示
│ ░░░│              │░░░░░░░░░░░░░░░░░░ │
│ ░░░└──────────────┘░░░ 🔍放大镜 ░░░░ │
│ ░░░░░░░┌───────────────────────┐░░░░ │
│ ░░░░░░░│ 工具条(见下)          │░░░░ │
│ ░░░░░░░└───────────────────────┘░░░░ │
└────────────────────────────────────────┘
```

### 4.2 标注工具条
```
┌──────────────────────────────────────────────────┐
│ ⬚矩形 ⬯椭圆 ➔箭头 ✏画笔 T文字 ▓马赛克 🔢序号 │
│  │ 🎨颜色 ━粗细 │ ↶撤销 ↷重做 │ ✕取消 📌钉 💾存 📋复制 ✓完成 │
└──────────────────────────────────────────────────┘
```
- 工具条自动避让屏幕边缘（选区靠下则显示在上方）
- 二级面板：选中某工具后弹出对应属性（颜色/粗细/字号）

### 4.3 贴图悬浮窗
```
 ┌────────────────┐
 │                │   ← 无边框，鼠标悬停显示细边框+阴影
 │    图片内容     │   ← 右键弹出上下文菜单
 │                │
 └────────────────┘
```

### 4.4 交互细节
- 截图时放大镜：跟随鼠标放大 8 倍局部，显示坐标与 HEX 取色值
- 所有悬浮窗统一投影阴影，符合 Win11 圆角风格（Mica/Acrylic 可选）
- 暗色/亮色主题跟随系统

---

## 5. 里程碑计划

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1 截图核心 | 区域截图 + 遮罩 + 框选 + 剪贴板/保存 | 可截图并保存 |
| M2 标注 | 文字/箭头/矩形/画笔/马赛克 + 撤销重做 | 可标注 |
| M3 贴图 | 钉住悬浮窗 + 拖动/缩放/透明度 + 管理 | 可贴图 |
| M4 系统 | 热键自定义 + 托盘 + 设置 + 自启 | 完整可用 |
| M5 打磨 | 智能窗口识别、放大镜、多屏、主题、打包 | 发布版 |

---

## 6. 风险与对策

| 风险 | 对策 |
|------|------|
| 高 DPI 多屏截图错位 | 使用 Per-Monitor V2 + 物理像素坐标换算，逐屏捕获 |
| 全局热键被占用 | 注册失败时提示用户更换，支持完全自定义 |
| 置顶窗被全屏游戏/独占模式遮挡 | 文档说明限制，提供"普通置顶"与"顶层置顶"两种 |
| Graphics Capture 兼容性 | 降级方案 `CopyFromScreen` |
| 马赛克性能 | 预降采样位图，仅对涂抹区域重绘 |
| 杀软误报注入/截屏 | 代码签名 + 开源透明 + 仅本地运行 |

---

## 7. 后续扩展（Roadmap）
- 滚动截图（长图拼接）
- 录屏 / GIF 录制
- OCR 文字识别（提取图中文字）
- 取色器独立工具
- 贴图分组 / 多桌面
- 云端图床上传分享
- 历史截图记录与检索
```
